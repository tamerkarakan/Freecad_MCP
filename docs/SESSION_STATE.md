# Session State

## Current Status

- Local git repository initialized on `main`.
- First baseline commit exists: `720e9f2 chore: initialize FreeCAD MCP inventory`.
- Repo discipline and verification commit exists: `7c6ef09 chore: add repo discipline and verification automation`.
- Phase 1 static MCP commit exists: `57930c3 feat: add static MCP server tools`.
- Phase 2 runtime bridge commit exists: `558b4b0 feat: add FreeCADCmd runtime bridge`.
- Typed FreeCAD MCP surface commit exists: `3dcc942 feat: complete typed FreeCAD MCP tool surface`.
- Remote repository is configured as `origin`: `https://github.com/tamerkarakan/Freecad_MCP.git`.
- First GitHub push completed to `origin/main`; repository was created private.
- FreeCAD upstream source is checked out under ignored `upstream/FreeCAD`.
- Static inventory currently scans 1112 GUI command registrations from FreeCAD commit `dee977f98f8a8542c8db0be2ecc529a771931d01`.
- MCP plan favors typed document/object/Part/Sketch tools plus lower-level command and Python escape hatches.
- Verification command: `scripts\verify.ps1`.
- MCP server entrypoint exists at `server.py`.
- Implemented static tools: `freecad_command_list`, `freecad_command_describe`, `freecad_source_symbol_index`, `freecad_source_search`, `freecad_source_open`.
- MCP client config example exists at `docs/MCP_CLIENT_CONFIG.md`.
- MCP tool schema snapshots are generated at `docs/mcp_tool_schemas.json` and `docs/mcp_tool_schemas.md`.
- Phase 2 FreeCADCmd bridge is implemented as process-per-call.
- Implemented runtime tools: `freecad_session_status`, `freecad_python_exec`.
- Typed CAD tools are implemented for document, object, Part, PartDesign, Sketcher, import/export, mesh, and Assembly basics.
- Sketcher typed tools now cover 13 tools: create, add geometry, add constraint, add profile, profile create, profile validate, curve fit analyze, geometry method catalog, edit geometry, edit constraints, transform, auto-constrain, and validate.
- Sketcher geometry support includes point, line, angle/length line, circle, 3-point circle, circular arc by center/angles, circular arc by 3 points, circular arc by start/end/radius, ellipse, ellipse arc, hyperbola arc, parabola arc, B-spline, and polyline creation.
- Sketcher profile helpers cover rectangle, center rectangle, 3-point rectangle, named regular polygons (`triangle`, `square`, `pentagon`, `hexagon`, `heptagon`, `octagon`), arbitrary regular polygon, circle, straight slot, oriented start/end slot, arc slot, and polyline.
- `freecad_sketch_add_geometry` and `freecad_worker_sketch_add_geometry` can now add ordered closed curve chains safely with `connect_sequence`, `close_sequence`, and `require_closed`, preventing reference-image traces from being saved as disconnected free geometry when a closed profile is required.
- `freecad_sketch_profile_create`/`freecad_sketch_profile_validate` and worker equivalents now provide the stricter production-profile path: ordered loop segments, endpoint drift rejection before constraints, curve-preservation contracts that can reject line/polyline fallback, segment/geometry intent mismatch reporting, native geometry type counts, Part face validation, isolated point checks, branch endpoint checks, micro-offset checks, and optional full-constraint validation.
- `freecad_curve_fit_analyze` compares traced points against line and circular-arc fits and recommends line, arc, or B-spline/freeform before a sketch is created.
- `freecad_sketch_geometry_method_catalog` reports supported Sketcher creation methods, and circular arc creation now returns actual start/end, center, radius, sweep degrees, and normal in `geometry_reports` for both process-per-call and worker sketch tools.
- `freecad_sketch_create` and `freecad_sketch_profile_create` can now create/reuse a PartDesign Body and attach the sketch to a Body origin plane (`XY`, `XZ`, `YZ`), matching the UI flow that asks for Body creation and plane selection before PartDesign features.
- `freecad_partdesign_body_create`/`freecad_partdesign_pad`/`freecad_partdesign_pocket`/`freecad_partdesign_hole`/`freecad_partdesign_revolution`/`freecad_partdesign_groove` and worker equivalents provide the first PartDesign typed slice: Body creation/reuse, Pad creation from an attached sketch, Pocket subtraction, plain Hole creation from a circle sketch, additive Revolution from a sketch axis, and subtractive Groove from a sketch axis, with optional solid-result enforcement and Body Tip reporting.
- Sketcher diagnostics now report solver result, DoF, open vertices, conflicts/redundants/malformed constraints, missing constraints, dependent geometry, and optional per-constraint errors.
- Sketcher `Group` and `Text` raw constraints are blocked by typed tools because direct constructor attempts can terminate FreeCADCmd in the current FreeCAD 1.1.1 runtime; smoke coverage asserts safe structured failure.
- `freecad_part_extrude` now converts closed wire/sketch profiles to a planar `Part.Face` before extrusion, so closed Sketcher rectangles produce solid Part extrusions instead of shell-only results.
- `freecad_part_extrude` and `freecad_worker_part_extrude` now support opt-in parametric `Part::Extrusion` feature mode for explicit shell-only/solid, symmetric, taper, forward/reverse length, reverse direction, and face-maker options.
- Runtime shape summaries now keep a structured record for FreeCAD present-but-null `Shape` values (`is_null=true`, zero topology counts, no bound box), which protects process-per-call and persistent-worker tool consumers from shell-only feature edge cases in FreeCAD 1.1.1.
- `freecad_mesh_repair` repairs a mesh copy and assigns it back to avoid immutable mesh errors on imported `Mesh::Feature` objects; unsupported-only action lists now return structured errors without mutating/recomputing the mesh document.
- Long process-per-call runtime scripts now run through a temporary `.py` file when the inline `FreeCADCmd -c` command would exceed the Windows command-line limit.
- Runtime execution payloads now truncate oversized `argv`/`stdout`/`stderr` previews with total-length metadata to reduce oversized MCP responses.
- Runtime execution payloads also support opt-in compact metadata: `freecad_python_exec` uses `compact_output=true`, and typed CAD process-per-call tools use `compact_execution=true`.
- `FreeCadCmdBridge.execute_python` now returns structured launch failures (`launch_error`) for process start errors instead of propagating raw OS exceptions.
- Stdio server response writing now has a serialization fallback so non-JSON-serializable tool payloads return a structured internal error instead of terminating the server loop.
- MCP resource template listing is supported with an empty `resourceTemplates` response for clients that probe `resources/templates/list`.
- `scripts/smoke_static_mcp.py` now smoke-checks `resources/templates/list`.
- Product-style module filtering is implemented through `freecad_mcp.module_registry` and `FREECAD_MCP_MODULES`; default `all` preserves the full surface, while aliases such as `free`, `pro`, `studio`, `team`, and `source` expose narrower package-style tool sets. `dev`, `developer`, and `local-dev` intentionally remain full-surface aliases for local maintainer work. Worker tools require the `worker` module even when they also have domain tags, source-intelligence uses the `source` add-on alias, and unsafe Python exec remains an explicit `unsafe` add-on for product bundles.
- Sellable bundle descriptors live in `freecad_mcp.product_bundles`; generated outputs are `docs/PRODUCT_BUNDLES.md` and `docs/product_bundles.json`, exposed as MCP resources. Current generated counts are Free 23, Pro 70, Studio 133, Team 136, Source add-on 5, and Unsafe add-on 1.
- Distribution profile descriptors live in `freecad_mcp.distribution_profiles`; generated outputs are `docs/DISTRIBUTION_PROFILES.md`, `docs/distribution_profiles.json`, `packaging/README.md`, and `packaging/profiles/*.mcp.json`, exposed as MCP resources where applicable.
- `pyproject.toml` now declares setuptools build metadata, `src` package discovery, `freecad_mcp/runtime_scripts/*.py` package data, and the `freecad-hybrid-mcp` console entrypoint. Installed entrypoint runs can set `FREECAD_MCP_REPO_ROOT` when resources/docs live outside the current working directory.
- `scripts/smoke_python_package.py` builds and inspects wheel and sdist artifacts, verifies both runtime scripts plus the `freecad-hybrid-mcp` entrypoint are present, installs the wheel into a temporary venv, starts the installed entrypoint outside the repo cwd with `FREECAD_MCP_REPO_ROOT`, and verifies MCP initialize/tool calls under the `free` profile.
- Headless typed CAD tools are split into `freecad_mcp.cad_tool_base` plus domain services under `freecad_mcp.cad_domains`; `freecad_mcp.cad_tools.CadToolService` remains the backward-compatible aggregate over 60 CAD tools.
- `scripts/smoke_cad_tools.py` now covers STEP export/import roundtrip, Part boolean fuse + BOP geometry check, PartDesign Body-attached sketch profiles with Pad solid creation, Pocket subtraction, plain Hole creation from a circle sketch, additive Revolution, and subtractive Groove, unsupported mesh repair action reporting, open-sketch extrusion, advanced parametric extrusion, safe Sketcher Group/Text constraint blocking, current solid-shape extrude failure behavior, connected closed Sketcher curve chains, loop-based pad-ready profile creation/validation, native geometry type reporting, circular arc method catalog/reporting, profile method catalog aliases, rectangle/slot/polygon profile helpers, segment intent mismatch rejection, endpoint drift rejection, curve-preservation and line-fallback rejection, curve fit recommendation, and expanded Sketcher advanced geometry/profile/constraint/diagnostic/transform flows.
- `scripts/smoke_fixture_documents.py` generates a multi-object fixture document at verification time and checks object metadata, boolean geometry, Sketcher profile extrusion, Assembly links, reopen/list/get, geometry check, STEP export, and STL export without committing binary CAD fixtures.
- Persistent `freecadcmd-worker` mode is implemented for session lifecycle, in-memory document lifecycle, object list/get, and primitive creation.
- Persistent worker tools include roadmap aliases `freecad_session_start`, `freecad_session_list`, `freecad_session_close` plus explicit `freecad_worker_*` tools.
- Persistent worker coverage now includes document export, object set/delete, Part boolean/extrude/revolve/check-geometry, PartDesign Body/Pad/Pocket/Hole/Revolution/Groove, Sketcher create/add geometry/add constraints/add profiles/profile create/profile validate/edit/transform/auto-constrain/validate, mesh import/export/evaluate/repair/boolean, and Assembly create/insert/native joint proxy/solve/BOM tools.
- `scripts/smoke_persistent_worker.py` now exercises PartDesign Body/Pad/Pocket/Hole plus early Sketcher connected closed chains, circular arc method/reporting coverage, curve-preserving profile creation/rejection, native geometry type reporting, and segment intent mismatch rejection, then restarts into clean worker sessions for PartDesign Revolution/Groove, Sketcher edit/transform validation, and later object, Part, mesh, and Assembly flows.
- Persistent worker startup also uses a temp `.py` script for long worker payloads and keeps that file until session close to avoid Windows script-load races.
- Persistent worker manager now drops and cleans up crashed/stopped sessions after request/status errors, with fake-worker crash injection unit coverage.
- `freecad_assembly_create_joint` now creates native Assembly `JointObject.Joint` proxies instead of plain placeholder string metadata.
- Current MCP tool count is 137 when `FREECAD_MCP_MODULES=all`.
- MCP resources and prompts are implemented in `freecad_mcp.mcp_stdio`; product modules, product bundle manifests, distribution profile manifests, and the Workbench artifact manifest are exposed as read-only resources.
- Roadmap status is finalized in `docs/ROADMAP_STATUS.md` and exposed as an MCP resource; there are no unblocked roadmap items left in the current scope.
- GUI attach design for active document, active view, selection, preselection, and subelement records is documented in `docs/GUI_ATTACH_PLAN.md` and exposed as an MCP resource.
- FreeCAD 1.1/1.1.1 GUI research and product priorities are documented in `docs/GUI_1_1_1_RESEARCH.md` and exposed as MCP resource `freecad://docs/gui-1-1-1-research`: 1.1.1 is a patch release over 1.1, local command inventory counts 1112 GUI command registrations, and GUI expansion should prioritize Sketcher + PartDesign, then TechDraw, then Assembly, then visual assist helpers, with CAM/FEM kept behind fixture-backed advanced contracts.
- Screenshot/vision debugging policy is documented in `docs/VISION_DEBUG_PIPELINE.md` and exposed as an MCP resource: structured tool data first, narrow programmatic GUI actions before mouse actions, local screenshot evidence by default, crop/detail escalation only as needed, and user confirmation for ambiguous curve intent.
- GUI attach client/server slice is implemented: `scripts/freecad_gui_bridge_server.py` runs inside FreeCAD GUI, while MCP tools expose attach/list/detach/status, active document/view reads, selection/preselection reads, selection set, view fit, a first typed GUI primitive creation path for cylinders, user-visible object Label updates, first-priority Sketcher/PartDesign state readers, Sketcher edit enter/leave, PartDesign Body activation, and feature-task state (`freecad_gui_object_label_set`, `freecad_gui_sketch_state`, `freecad_gui_sketch_enter`, `freecad_gui_sketch_leave`, `freecad_gui_partdesign_state`, `freecad_gui_body_activate`, `freecad_gui_feature_task_state`) for active edit object, DoF/diagnostics, Body/Tip, task-panel, and feature-chain inspection/control.
- GUI bridge shape summaries are guarded for invalid/null GUI object shapes, so empty Sketcher state reads do not fail on FreeCAD `shape is invalid` errors.
- Opt-in GUI attach smoke exists at `scripts/smoke_gui_attach.py`; it launches FreeCAD GUI, starts the bridge with a token, selects two `Face1` records, reads them through `freecad_gui_selection_get`, sets a GUI object Label, enters/leaves a GUI sketch, reads feature-task state, activates a PartDesign Body, calls view fit, closes the GUI process, and creates a Fixed Assembly joint with populated `Reference1`/`Reference2`. Last local run passed with report under `runs/gui-smoke/`.
- Workbench-hosted bridge mode exists at `freecad_workbench/FreeCADMCP/InitGui.py`; it registers a **FreeCAD MCP** workbench with start/stop/status commands and optional `FREECAD_MCP_AUTOSTART=1` hosting. Setup notes are in `docs/WORKBENCH_BRIDGE.md`.
- A compatibility `freecad_workbench/InitGui.py` shim is present because FreeCAD 1.1.1 treats `-M` additional module paths as flat module directories; launching with either `-M freecad_workbench` or `-M freecad_workbench/FreeCADMCP` should register the same workbench. Both InitGui files derive their path from the compiled code filename when `__file__` is unavailable.
- `freecad_mcp.workbench_artifact` describes the local `freecad-mcp-workbench.zip` module artifact for Pro/Studio/Team profiles. `scripts/build_workbench_addon.py` generates `docs/WORKBENCH_ARTIFACT.md`, `docs/workbench_artifact.json`, and `packaging/workbench/README.md`; with `--zip-out`, it embeds `FreeCADMCP/InitGui.py`, `FreeCADMCP/freecad_gui_bridge_server.py`, `FreeCADMCP/workbench_artifact.json`, and `FreeCADMCP/README.md`.
- TechDraw/CAM/FEM design review is captured in `docs/TECHDRAW_CAM_FEM_PLAN.md`; first typed slices are implemented for TechDraw page/template/view/DXF, CAM raw `Path::Feature` command paths + G-code export, and FEM analysis/material/fixed-force constraints without solver execution.
- The stdio server now runs on the official `mcp` SDK (`mcp.server.lowlevel.Server` via `mcp_stdio.create_mcp_server`/`serve`); the hand-rolled JSON-RPC dispatcher was removed and `mcp>=1.0` is a project dependency. Tool argument validation stays in handlers (`validate_input=False`) so tool behavior/schemas are unchanged; tool errors use the SDK isError shape.
- Opt-in structured JSON logging exists in `freecad_mcp.logging_config` (env `FREECAD_MCP_LOG_LEVEL`/`FREECAD_MCP_LOG_FILE`): stderr-only so stdout stays protocol-safe, redacts argument values, logs tool-call timing and worker lifecycle.
- `freecad_session_console` (alias `freecad_worker_console_read`) reads captured worker FreeCAD console output (App.Console stdout messages + stderr warnings/errors) without broad Python exec.
- `freecad_curve_fit_analyze` returns an image-to-sketch decision report (candidates/confidence/ambiguous/ask_user) instead of silently degrading curve intent.
- `freecad_object_rename_label`, `freecad_worker_object_rename_label`, and `freecad_gui_object_label_set` set user-visible object labels while keeping internal FreeCAD `Name` values stable; CAD tool count 60.
- `freecad_partdesign_pocket`/`freecad_worker_partdesign_pocket`, `freecad_partdesign_hole`/`freecad_worker_partdesign_hole`, and `freecad_partdesign_groove`/`freecad_worker_partdesign_groove` cut material from an existing Body solid (verifies the Body Tip is a solid first); `freecad_partdesign_revolution`/`freecad_worker_partdesign_revolution` provide additive revolved solids from sketch axes.
- `source_search` is bounded with `max_files`/`time_budget_sec` and lazy `os.walk` early termination.
- A CI workflow exists at `.github/workflows/ci.yml` (Linux unit suite + static smoke, asserts the symlink test runs) but is untracked: pushing it needs a remote credential with the `workflow` OAuth scope.
- New expansion TODOs are captured in `docs/BACKLOG.md`: remote HTTP/SSE/Streamable HTTP MCP endpoint, deeper structured crash/performance logging, console reading beyond the persistent worker, fuller live GUI bridge/task-panel access, richer image-to-sketch prompt/resource guidance, deeper Sketcher/PartDesign workbench coverage from docs plus source, and deciding whether generated distribution profiles become separate packages/plugins/addons.
- The two large embedded FreeCAD runtime scripts were extracted from inline triple-quoted literals into `src/freecad_mcp/runtime_scripts/{cad_action.py,worker.py}` and are loaded at import via `tooling.load_runtime_script`; `cad_tools.py` is now a thin aggregate while shared process-per-call execution lives in `cad_tool_base.py` and domain definitions live in `cad_domains/`. These script files are package data and must be committed/shipped alongside the modules that load them; the loader raises a clear error if either is missing.
- `static_tools.safe_source_path` was hardened to a single authoritative resolved-path `is_relative_to` containment check plus lexical `..`/absolute rejection, replacing the prior resolve-then-separate-symlink-walk (removes the two-pass race).
- The persistent worker manager now caps concurrent sessions (`FREECAD_MCP_MAX_WORKER_SESSIONS`, default 8) and registers an `atexit` cleanup so worker processes and their temp scripts are torn down on normal interpreter exit even if the stdio shutdown path is bypassed.
- Current unit coverage includes `tests/unit/test_cad_tools.py` guards for CadToolService host-side dispatch/validation, the CAD domain-service split, `CAD_ACTION_SCRIPT` integrity, runtime-script loading, and null-shape summary metadata; `test_gui_bridge.py` guards GUI attach delegation including Sketcher/PartDesign state, edit, activation, and feature-task tools; `test_distribution_profiles.py` guards distribution profiles and pyproject packaging declarations; `test_workbench_artifact.py` guards the generated Workbench artifact manifest; worker session-cap and worker-script-integrity/null-shape guards in `test_persistent_bridge.py`; and `safe_source_path` containment/escape guards in `test_static_tools.py`. Three previously tautological persistent-bridge selector-guard tests were de-tautologized with a real worker session plus message-matched assertions.

## Next Session Checklist

1. Run `git status --short --branch`.
2. Run `scripts\verify.ps1`.
3. Read `docs/BACKLOG.md` and `docs/BUGS.md`.
4. Re-run a Sketcher profile smoke if extrude behavior changes again.
5. Run unit tests for `runtime_bridge` and `mcp_stdio` when touching transport/serialization code.
6. Run persistent worker smoke when touching session lifecycle or worker tools.
7. Use `docs/ROADMAP_STATUS.md`, `docs/GUI_1_1_1_RESEARCH.md`, `docs/GUI_ATTACH_PLAN.md`, `docs/VISION_DEBUG_PIPELINE.md`, `docs/WORKBENCH_BRIDGE.md`, `docs/WORKBENCH_ARTIFACT.md`, and `docs/TECHDRAW_CAM_FEM_PLAN.md` before extending GUI/vision/Workbench/TechDraw/CAM/FEM tools.
8. Push only after verification passes.
